import 'package:flutter/cupertino.dart';

class FcGameWidget extends StatelessWidget {
  const FcGameWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("开发中。。。"),
    );
  }
}
